URL: http://localhost:8000/api/v1/text

post command:

{
  "Text":"It looks like you're using PowerShell, which doesn't recognize the which command that's common in Unix-like environments. In PowerShell, you should use the Get-Command cmdlet instead."
}

Install:
Django,gtts